# Local development state
node_modules
.a0proj
.ghost-cli
*.log
.DS_Store

# Ghost local state
content/logs
content/data/ghost.db
config.development.json

# Keep core files but ignore built artifacts if any
# (Ghost usually ships built files in its versions/core/frontend/public directory already)
